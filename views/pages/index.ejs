<!DOCTYPE html>

    <%- include('../partials/head'); %>
<body class="container">
    <%- include('../partials/header', {pagina: 'Home'}); %>

    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com EJS</p>
            <p><%= subtitles %></p>
        </div>
        <ul>
            

            <!-- o meu qualitys ta pegando tudo do meu objeto la no JS -->
            <!-- quanto eu for usar JS no EJS, não precisa usar o - <% %>-->
            <!-- para usar HTML dentro do JS, tem que fechar a tag da função -->
            <% qualitys.forEach(function(quality){ %> 
                <li><strong><%= quality.titulo %></strong><%= quality.mensagem %></li>
            <% }) %>
        </ul>
    </main>

    <%- include('../partials/footer'); %>

</body>
</html>